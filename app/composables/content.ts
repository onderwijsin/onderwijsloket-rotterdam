import { slides, cards, activity, advies, routes, stories, partners, contact } from "~/assets/content"

export const useContent = () => {

  return { slides, activity, cards, advies, routes, stories, contact, partners }
}
