/** @type {import('tailwindcss').Config} */

export const theme = {
  extend: {
    fontFamily: {
      sans: ['F37Ginger', 'sans-serif'],
      serif: ['Noto Serif', 'serif'],
    },
  }
}

export default {
  theme,
};